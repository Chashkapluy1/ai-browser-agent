# AI Browser Agent

Автономный AI-агент для автоматизации браузера, способный выполнять сложные многошаговые задачи без предопределенных сценариев.

## 🎯 Описание

AI Browser Agent — это интеллектуальный агент, который:
- Автономно управляет веб-браузером через Playwright
- Анализирует содержимое страниц и принимает решения в реальном времени
- Использует OpenAI GPT-4o для понимания задач и планирования действий
- Динамически находит интерактивные элементы без использования статических селекторов
- Работает полностью автономно до завершения задачи или запроса дополнительной информации

## 🏗️ Архитектура

Проект построен на принципах ООП и модульной архитектуры:

```
ai_browser_agent/
├── agent/
│   ├── browser_controller.py  # Управление браузером (Playwright)
│   ├── page_analyzer.py       # Анализ и разметка DOM
│   └── core.py                # Ядро AI-агента (цикл принятия решений)
├── tools/
│   ├── browser_tools.py       # Инструменты для взаимодействия с браузером
│   └── tool_manager.py        # Менеджер инструментов (автогенерация схем)
├── scripts/
│   ├── analyze_page.js        # JS-скрипт для разметки DOM
│   ├── get_page_text.js       # JS-скрипт для извлечения текста страницы
│   └── get_page_data.js       # Оптимизированный JS для сбора данных
├── prompts/
│   └── system_prompt.txt      # Системный промпт для LLM
├── utils/
│   └── logger.py              # Настройка логирования
├── main.py                     # Точка входа
├── requirements.txt            # Зависимости
└── README.md                   # Документация
```

### Ключевые компоненты

1. **BrowserController** — управляет жизненным циклом браузера, навигацией и базовыми операциями
2. **PageAnalyzer** — анализирует DOM, исполняя внешние JS-скрипты для разметки интерактивных элементов уникальными идентификаторами (`data-ai-id`)
3. **BrowserTools** — набор `async` методов, представляющих собой действия агента (клик, ввод текста и т.д.)
4. **ToolManager** — управляет регистрацией и вызовом инструментов. **Автоматически генерирует схемы** для OpenAI API на основе сигнатур функций и docstrings
5. **AICore** — мозговой центр системы, реализует цикл "наблюдение → мысль → действие" и управляет контекстом беседы с LLM

## 🚀 Установка

### Требования

- Python 3.10+
- OpenAI API ключ

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd ai_browser_agent
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Установите браузеры для Playwright:
```bash
playwright install chromium
```

5. Создайте файл `.env` и добавьте ваш API ключ:
```bash
OPENAI_API_KEY=your_openai_api_key_here
```

## 💻 Использование

Запустите агента:
```bash
python main.py
```

Введите задачу текстом, например:
- "Найди вакансии Python разработчика на hh.ru"
- "Открой Google и найди информацию о Python"
- "Зарегистрируйся на сайте example.com"

Агент будет автономно выполнять задачу, показывая свои действия в открывшемся окне браузера.

## 🔧 Технологический стек

- **Python 3.10+** — основной язык программирования
- **Playwright** — автоматизация браузера
- **OpenAI API (GPT-4o)** — языковая модель для принятия решений
- **python-dotenv** — управление переменными окружения

## ✨ Особенности реализации

### Продвинутые паттерны

1. **Динамическая разметка элементов** — агент сам находит интерактивные элементы и присваивает им уникальные идентификаторы, избегая использования статических селекторов
2. **Автоматическая генерация схем для Tool Calling** — вместо ручного описания JSON-схем, система автоматически генерирует их из docstrings и сигнатур функций, что исключает ошибки и соответствует принципу DRY (Don't Repeat Yourself)
3. **Автономный цикл принятия решений** — агент самостоятельно планирует последовательность действий на основе анализа страницы, управляя при этом окном контекста для оптимизации

### Соответствие стандартам

- ✅ **PEP 8** — код соответствует стандартам стиля Python
- ✅ **Flake8** — проходит проверку линтером
- ✅ **ООП** — модульная архитектура с четким разделением ответственности
- ✅ **Type hints** — использование аннотаций типов для улучшения читаемости и поддержки

## 🛠️ Разработка

### Добавление новых инструментов

Архитектура спроектирована для максимальной расширяемости. Чтобы добавить новый инструмент, достаточно выполнить всего один шаг:

1. Добавьте новый `async` метод в класс `BrowserTools` (`tools/browser_tools.py`), снабдив его аннотациями типов и docstring в стиле Google.

Всё. Система автоматически "обнаружит" новый метод, сгенерирует для него JSON-схему на основе его сигнатуры и docstring, и сделает его доступным для AI-агента. Никаких ручных правок в других файлах не требуется.

## 👤 Автор

**Имя:** Павел Лагерев

- **GitHub:** [Chashkapluy1](https://github.com/Chashkapluy1)
- **Telegram:** [@Chashkapluy](https://t.me/Chashkapluy)

## 📄 Лицензия

Этот проект создан в рамках тестового задания.

---

**Примечание**: Убедитесь, что у вас есть валидный OpenAI API ключ с доступом к GPT-4o для работы агента.
